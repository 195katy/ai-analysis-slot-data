services:
  terraform:
    build:
      context: .
    volumes:
      - ../../:/workspace:cached
      - ~/.aws:/root/.aws
      - /var/run/docker.sock:/var/run/docker.sock
    command: sleep infinity
